import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { withKnobs, text, boolean, select } from '@storybook/addon-knobs';
import { ToggleGroup } from '../ToggleGroup';

export const items = [
  {
    label: 'Item A',
    value: 'itema',
  },
  {
    label: 'Item B',
    value: 'itemb',
  },
  {
    label: 'Item C',
    value: 'itemc',
  },
];

export const onChange = item => console.log(`The item value is '${item.value}' and the label is '${item.label}'`);

<Meta title="Components/ToggleGroup" component={ToggleGroup} />

# ToggleGroup

## Description

ToggleGroup.

## Preview

<Preview>
  <Story name="Default">
    <ToggleGroup items={items} onChange={onChange} selectedItem={items[1]} />
  </Story>
</Preview>
