import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { NotificationBar } from '../NotificationBar';
import { StyledNotificationIcon, StyledUpgradeIcon } from './NotificationBar.styles';


<Meta title="Components|Notification Bar" />

# Notification Bar

## Description

A notification bar component to display notification to the user on the page

### Default

<Preview>
  <Story name="info">
    <NotificationBar variant='info'>
      <div style={{display: 'flex', alignItems: 'center'}}>
        <StyledNotificationIcon />
        <span>
          You are currently using an outdated browser, this may cause issues using our site. Upgrade to a modern browser
          for a better experience
        </span>
      </div>
      <button type="button" style={{ backgroundColor: 'red' }}>
        <StyledUpgradeIcon />
        upgrade
      </button>
    </NotificationBar>
  </Story>
</Preview>

<Preview>
  <Story name="alert">
    <NotificationBar variant='alert'>
      <div style={{display: 'flex', alignItems: 'center'}}>
        <StyledNotificationIcon />
        <span>
          You are currently using an outdated browser, this may cause issues using our site. Upgrade to a modern browser
          for a better experience
        </span>
      </div>
      <button type="button" style={{ backgroundColor: 'red' }}>
        <StyledUpgradeIcon />
        upgrade
      </button>
    </NotificationBar>
  </Story>
</Preview>

<Preview>
  <Story name="default">
    <NotificationBar>
      You are currently using an outdated browser, this may cause issues using our site. Upgrade to a modern browser for a better experience
    </NotificationBar>
  </Story>
</Preview>


