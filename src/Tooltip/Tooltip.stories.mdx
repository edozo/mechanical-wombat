import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { Tooltip } from '../Tooltip';

<Meta title="Components|Tooltip" />

# Tooltip

## Description

A tooltip component to display information on hover

### Default

<Preview>
  <Story name="Default">
    <Tooltip content={<div>
      <span>Tooltip</span>
    </div>}>
      <button type="button">the child</button>
    </Tooltip>
  </Story>
</Preview>

<Preview>
  <Story name="Interactive">
    <Tooltip interactive={true} content={<div>
      <span>Tooltip</span>
      <button type="button">content</button>
    </div>}>
      <button type="button">the child</button>
    </Tooltip>
  </Story>
</Preview>
