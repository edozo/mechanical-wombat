import { DefaultTheme } from 'styled-components';

export const defaultTheme: DefaultTheme = {
  name: 'Default Theme',
  borderRadius: '5px',
  colors: {
    primary: 'green',
    secondary: 'blue',
  },
};
