import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { withKnobs, text, boolean, select } from '@storybook/addon-knobs';
import { DisplayBox, DisplayBoxWrapper } from '../DisplayBox';
import { DisplayBoxTitle, DisplayBoxSubTitle, DisplayBoxBody } from '../DisplayBox/DisplayBox.styles.ts';
import { ZoomInIcon, ZoomOutIcon, SaveIcon, TrashCanIcon, NotificationIcon, UpgradeIcon, RefreshIcon } from '../Icons';

export const icons = [
  { name: 'Trash Can', component: <TrashCanIcon /> },
  { name: 'Zoom In', component: <ZoomInIcon /> },
  { name: 'Zoom Out', component: <ZoomOutIcon /> },
  { name: 'Save', component: <SaveIcon /> },
  { name: 'Notification', component: <NotificationIcon /> },
  { name: 'Upgrade', component: <UpgradeIcon /> },
  { name: 'Refresh', component: <RefreshIcon /> },
];

<Meta title="Components|Icons" />

# Icons

## Description

Icons (POC) can be used as a react component using the standard naming convetions (`<ZoomIn />` for example), or as a base64 string to be used in a HTML `img` tag (`<img src={zoomInUrl} />`)

<Story name="Default">
  <DisplayBoxWrapper>
    {icons.map(icon => (
      <div style={{padding: '8px', textAlign: 'center'}}>
        {icon.component}
        <DisplayBoxSubTitle>{icon.name}</DisplayBoxSubTitle>
      </div>
    ))}
  </DisplayBoxWrapper>
</Story>
